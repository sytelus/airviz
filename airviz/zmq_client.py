from zeroless import Client

class ZmqClient:
    def __init__(self):
        self.client = client()
    def start(port):
        self.client.connect_local(port=port)
    def get_sub(topic):
        client.sub(topics=[topic])